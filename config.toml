[model]
# Local model directory after setup.sh download
dir = "models/cardiffnlp-roberta-sentiment"
device = "cpu"

[inference]
# Inference parameters
max_length = 128
batch_size = 8
num_labels = 3
labels = ["negative", "neutral", "positive"]
